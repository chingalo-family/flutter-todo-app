import 'package:flutter/material.dart';

class TodoListDetail extends StatelessWidget {
  TodoListDetail({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Todo List detail ');
  }
}
